# shorkariiq

An adaptive learning quiz app.

## Gemini API key

The chat feature expects a `GEMINI_API_KEY` at runtime. Pass it with `--dart-define` when running the app, for example:

```bash
flutter run --dart-define=GEMINI_API_KEY=your_key_here
```

